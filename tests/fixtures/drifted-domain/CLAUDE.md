# Claude Code Instructions

Read [AGENTS.md](./AGENTS.md). All cross-IDE instructions live there to avoid drift between Claude/Codex/Cursor convention files.

Project-specific Claude Code config (permissions, hooks, custom agents/skills/commands) lives under `.claude/`.
